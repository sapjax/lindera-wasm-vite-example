# Lindera WASM Vite Example
This is an example project demonstrating how to use Lindera's WebAssembly (WASM) tokenizer with Vite. Lindera is a Japanese morphological analyzer and tokenizer.

## Development
- npm run dev